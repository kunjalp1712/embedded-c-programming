#include <stdio.h>
#include <string.h>

int main()
{
    char sensor_name[] = "IR Sensor";

    printf("Sensor Name: %s\n", sensor_name);
    printf("Length: %lu\n", strlen(sensor_name));

    return 0;
}
